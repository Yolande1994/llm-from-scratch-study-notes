前置知识补全（Preliminaries）
目录定位
本目录为个人原创扩展内容，对应原书第 1 章「理解大型语言模型」的理论部分，补充可运行的实践代码，搭建从静态词嵌入 → Transformer 动态上下文表示的完整前置学习链路，为后续 GPT 自回归模型的学习夯实底层原理基础。
所有实现均为教学极简版本，重点拆解底层数学原理与数据流转逻辑，不做工业级工程封装，适合从零理解 LLM 底层演进逻辑。
1. Word2Vec Skip-Gram 三种梯度实现
通过三种不同复杂度的实现方式，循序渐进理解词向量的底层训练逻辑，对应「理解数学原理 → 掌握向量化优化 → 熟悉工业框架写法」的标准学习路径。
文件说明
表格
文件名	依赖	核心特点	学习目标
Word2Vec_python.py	仅 Python 标准库	纯手写矩阵运算与反向传播，逐行对应数学公式，无任何黑箱调用	吃透 Skip-Gram 的前向计算、损失函数、反向传播梯度推导
Word2Vec_numpy.py	NumPy	向量化矩阵运算实现，替代手写循环，对比朴素版直观体现向量化的性能优势	掌握 NLP 中向量化编程思维，理解矩阵运算对计算效率的提升
Word2Vec_pytorch.py	PyTorch	基于自动微分的标准框架实现，遵循 PyTorch 训练规范	熟悉工业界模型开发流程，理解自动微分与手动反向传播的对应关系
运行方式
直接执行对应 Python 文件即可，无需额外参数：
bash
运行
python Word2Vec_python.py
核心结论
训练后语义相近的词（水果类、动物类）余弦相似度显著更高，验证了分布式语义假设：词的含义由其上下文决定，向量空间的相对位置对应语义的相似性。
2. Transformer 编码器文本分类
从零手写完整 Transformer 编码器，基于二分类任务验证自注意力机制的核心能力，承接 Word2Vec 静态词嵌入，直观演示动态上下文表示的核心价值。
文件说明
Transformer_classifier.py：单文件完整可运行，所有核心组件从零实现，无nn.Transformer等黑箱调用。
核心内容
全组件手写实现：正弦余弦位置编码、缩放点积注意力、多头注意力、前馈网络（FFN）、残差连接 + 层归一化，完整复现 Transformer 编码器核心逻辑。
核心对比演示：
同一词语在不同语境下的词向量差异，直观展示 Transformer 动态词嵌入与 Word2Vec 静态词向量的本质区别
自注意力权重可视化，清晰呈现模型对词间语义关联的学习结果
完整训练闭环：包含数据预处理、模型训练、准确率计算、结果分析全流程。
运行方式
bash
运行
python Transformer_classifier.py
关键结论
Transformer 通过自注意力机制实现了上下文相关的动态词表示，解决了 Word2Vec 静态词向量无法处理多义词的核心缺陷，是 GPT 类自回归语言模型的核心基础。
学习路径衔接
本目录内容对应整个学习链路的第一阶段：基础原理筑基
前置基础：静态词嵌入 → Transformer 动态上下文表示（本目录）
核心模型：GPT-2 完整架构、预训练、微调（原书 ch02~ch07）
推理优化：KV Cache、精度优化等进阶实践（extras-inference目录）
所有代码均配套逐行中文原理注释，标注张量形状变化与设计逻辑，可按顺序递进学习，完整覆盖 LLM 从底层到上层的核心知识体系。
